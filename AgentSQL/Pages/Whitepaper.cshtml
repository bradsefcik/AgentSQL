@page
@model AgentSQL.Pages.WhitepaperModel
@{
    ViewData["Title"] = "AgentSQL Whitepaper";
}

<div class="container">
    <div class="paper-content">
        @Html.Raw(Model.WhitepaperContent)
    </div>
</div>

<style>
.paper-content {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem;
    line-height: 1.6;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

.paper-content h1 {
    color: #2c3e50;
    border-bottom: 3px solid #3498db;
    padding-bottom: 0.5rem;
    margin-bottom: 2rem;
}

.paper-content h2 {
    color: #34495e;
    margin-top: 2rem;
    margin-bottom: 1rem;
    border-left: 4px solid #3498db;
    padding-left: 1rem;
}

.paper-content h3 {
    color: #34495e;
    margin-top: 1.5rem;
    margin-bottom: 0.8rem;
}

.paper-content ul, .paper-content ol {
    margin-left: 1.5rem;
    margin-bottom: 1rem;
}

.paper-content li {
    margin-bottom: 0.5rem;
}

.paper-content strong {
    color: #2c3e50;
}

.paper-content code {
    background-color: #f8f9fa;
    padding: 0.2rem 0.4rem;
    border-radius: 3px;
    font-family: 'Courier New', monospace;
}

.paper-content blockquote {
    border-left: 4px solid #bdc3c7;
    margin: 1rem 0;
    padding-left: 1rem;
    color: #7f8c8d;
}

.paper-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 1rem 0;
}

.paper-content th, .paper-content td {
    border: 1px solid #ddd;
    padding: 0.8rem;
    text-align: left;
}

.paper-content th {
    background-color: #f8f9fa;
    font-weight: 600;
}

@@media (max-width: 768px) {
    .paper-content {
        padding: 1rem;
    }
}
</style>